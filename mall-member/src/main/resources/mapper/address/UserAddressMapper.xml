<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mall.address.mapper.UserAddressMapper">

    <resultMap id="addressResultMap" type="com.mall.address.model.UserAddressEntity">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="receiverName" column="receiver_name"/>
        <result property="receiverPhone" column="receiver_phone"/>
        <result property="province" column="province"/>
        <result property="city" column="city"/>
        <result property="district" column="district"/>
        <result property="detailAddress" column="detail_address"/>
        <result property="addressLabel" column="address_label"/>
        <result property="defaultAddress" column="is_default"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <sql id="addressColumns">
        id, user_id, receiver_name, receiver_phone, province, city, district,
        detail_address, address_label, is_default, created_at, updated_at
    </sql>

    <select id="findByUserId" resultMap="addressResultMap">
        SELECT <include refid="addressColumns"/>
        FROM user_address
        WHERE user_id = #{userId}
        ORDER BY is_default DESC, updated_at DESC, id DESC
    </select>

    <select id="findDefaultByUserId" resultMap="addressResultMap">
        SELECT <include refid="addressColumns"/>
        FROM user_address
        WHERE user_id = #{userId}
          AND is_default = 1
        LIMIT 1
    </select>

    <select id="findByIdAndUserId" resultMap="addressResultMap">
        SELECT <include refid="addressColumns"/>
        FROM user_address
        WHERE id = #{id}
          AND user_id = #{userId}
        LIMIT 1
    </select>

    <insert id="insert" parameterType="com.mall.address.model.UserAddressEntity"
            useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO user_address (
            user_id, receiver_name, receiver_phone, province, city, district,
            detail_address, address_label, is_default, created_at, updated_at
        ) VALUES (
            #{userId}, #{receiverName}, #{receiverPhone}, #{province}, #{city}, #{district},
            #{detailAddress}, #{addressLabel}, #{defaultAddress}, #{createdAt}, #{updatedAt}
        )
    </insert>

    <update id="update">
        UPDATE user_address
        SET receiver_name = #{receiverName},
            receiver_phone = #{receiverPhone},
            province = #{province},
            city = #{city},
            district = #{district},
            detail_address = #{detailAddress},
            address_label = #{addressLabel},
            is_default = #{defaultAddress},
            updated_at = #{updatedAt}
        WHERE id = #{id}
          AND user_id = #{userId}
    </update>

    <update id="clearDefaultByUserId">
        UPDATE user_address
        SET is_default = 0, updated_at = CURRENT_TIMESTAMP(3)
        WHERE user_id = #{userId}
          AND is_default = 1
    </update>

    <update id="setDefault">
        UPDATE user_address
        SET is_default = 1, updated_at = CURRENT_TIMESTAMP(3)
        WHERE id = #{id}
          AND user_id = #{userId}
    </update>

    <delete id="delete">
        DELETE FROM user_address
        WHERE id = #{id}
          AND user_id = #{userId}
    </delete>
</mapper>
