<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mall.admin.mapper.AdminMapper">
    <resultMap id="adminMap" type="com.mall.admin.model.AdminEntity">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="passwordHash" column="password_hash"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>
    <select id="count" resultType="long">SELECT COUNT(*) FROM sys_admin</select>
    <select id="findByUsername" resultMap="adminMap">
        SELECT id, username, password_hash, status, created_at, updated_at
        FROM sys_admin WHERE username = #{username} LIMIT 1
    </select>
    <select id="findById" resultMap="adminMap">
        SELECT id, username, password_hash, status, created_at, updated_at
        FROM sys_admin WHERE id = #{id} LIMIT 1
    </select>
    <insert id="insert" parameterType="com.mall.admin.model.AdminEntity"
            useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO sys_admin(username, password_hash, status) VALUES(#{username}, #{passwordHash}, #{status})
    </insert>
</mapper>
