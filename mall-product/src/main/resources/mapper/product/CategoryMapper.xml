<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mall.product.mapper.CategoryMapper">

    <resultMap id="categoryResultMap" type="com.mall.product.model.CategoryEntity">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="code" column="code"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findEnabledCategories" resultMap="categoryResultMap">
        SELECT id, name, code, sort_order, status, created_at, updated_at
        FROM product_category
        WHERE status = 1
        ORDER BY sort_order ASC, id ASC
    </select>

    <select id="findAllCategories" resultMap="categoryResultMap">
        SELECT id, name, code, sort_order, status, created_at, updated_at
        FROM product_category
        ORDER BY sort_order ASC, id ASC
    </select>

    <select id="findById" resultMap="categoryResultMap">
        SELECT id, name, code, sort_order, status, created_at, updated_at
        FROM product_category
        WHERE id = #{id}
        LIMIT 1
    </select>

    <select id="findByCode" resultMap="categoryResultMap">
        SELECT id, name, code, sort_order, status, created_at, updated_at
        FROM product_category
        WHERE code = #{code}
        LIMIT 1
    </select>

    <insert id="insert" parameterType="com.mall.product.model.CategoryEntity"
            useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO product_category (name, code, sort_order, status)
        VALUES (#{name}, #{code}, #{sortOrder}, #{status})
    </insert>

    <update id="update" parameterType="com.mall.product.model.CategoryEntity">
        UPDATE product_category
        SET name = #{name}, code = #{code}, sort_order = #{sortOrder}, status = #{status},
            updated_at = CURRENT_TIMESTAMP(3)
        WHERE id = #{id}
    </update>

    <update id="updateStatus">
        UPDATE product_category
        SET status = #{status}, updated_at = CURRENT_TIMESTAMP(3)
        WHERE id = #{id}
    </update>

    <select id="countProducts" resultType="long">
        SELECT COUNT(*) FROM mall_product WHERE category_id = #{id}
    </select>

    <delete id="deleteById">
        DELETE FROM product_category WHERE id = #{id}
    </delete>
</mapper>
